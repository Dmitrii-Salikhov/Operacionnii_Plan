## 1.1.8 — CI coverage для updater

- Тесты потоковой загрузки и проверки доверенных URL в `updater.py`.
- Покрытие снова ≥90% — зелёная сборка релиза.

## 1.1.7 — Окно, поступления и примечания


- Сохранение размера и позиции окна между запусками.
- «Примечания» только в полном плане (лист «Поступление»); отдельный Excel поступлений — без этого столбца.
- Уточнён `.gitignore` для локальных конфигов (`calendars.json` и др.).

## 1.1.6 — Обновления и контраст Review

- Автообновление: ZIP качается потоком на диск (не в RAM), таймаут установки 10 мин, лог прогресса.
- Выпадающие списки в Review: светлый текст на тёмном фоне (контраст в светлой теме).
- CI: flake8 E203, mypy Optional, покрытие ≥90%.

## 1.1.5 — Примечания из календаря, «дж» алиас

- Извлечение скобочных пометок `(...)` в «Примечания» на листе «Поступление».
- Токен `дж` теперь обрабатывается через ручную правку/алиасы (можно выбрать соответствие в Review).
- «Место для переноса…» теперь считается служебной пометкой, а не пациентом.

## 1.1.4 — Хирурги, обновление, журнал

- Хирурги из настроек снова попадают в Excel после сохранения (фикс устаревшего импорта).
- Окно установки обновления: шаги, лог, подтверждение.
- Журнал: подробнее, автоочистка, прокрутка вниз, кнопка «Очистить».

## 1.1.3 — Cyrillic paths on Windows

- Исправлено сохранение Excel в папки/имена с кириллицей (`План операций…` вместо `РџР»Р°РЅ…`).
- Bridge читает stdin как UTF-8; Electron пишет RPC в UTF-8 Buffer.

## 1.1.2 — Windows UTF-8 bridge

- Исправлена ошибка `'charmap' codec can't encode character '\u2192'` на Windows.
- JSON-RPC bridge всегда пишет ответы в UTF-8; Electron задаёт `PYTHONUTF8` / `PYTHONIOENCODING`.
- Стрелки `→` в текстах настройки заменены на `->`.

## 1.1.1 — Security hardening

- IPC allowlist: RPC-методы, `openExternal` (только GitHub репозитория), `openPath` (base_dir + диалоги).
- Автообновление: TLS + доверенные URL + SHA-256; установщик `.cmd`/`tar` без PowerShell Bypass.
- Права на `credentials.json` / `token.pickle` только для текущего пользователя.
- Ошибки bridge без traceback в UI; подтверждение перед установкой обновления.

## 1.1.0 — Electron UI

- Интерфейс **Electron + React** (тема Slice, светлая/тёмная).
- Логика по-прежнему **Python** (JSON-RPC bridge / sidecar).
- Крупные кнопки «Сформировать план» и «Выгрузить телефоны» под панелью календаря.
- Выгрузка телефонов с выбором формата: `7957…` или без ведущей `7` (`957…`).
- Подсказки при наведении на кнопки панели.
- Автообновление Windows: zip + SHA-256, установщик без PowerShell Bypass.
- Мастер первого запуска и управление списком календарей из UI.
- Статус событий **по каждому calendar ID**.
- Ужесточение IPC: allowlist RPC / URL / путей; права на `credentials.json` / `token.pickle`.

См. [MIGRATION.md](MIGRATION.md), [SECURITY.md](SECURITY.md).

## Целостность

Проверьте `PlanOperaciy-Windows.zip.sha256`.
