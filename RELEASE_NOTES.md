## Исправления

- **Обновление на Windows:** исправлен `UnicodeEncodeError` при путях с кириллицей (скрипт обновления теперь UTF-8 с BOM вместо ASCII).

## Целостность

Проверьте `PlanOperaciy-Windows.zip.sha256` (SHA-256). Клиент обновлений откажется ставить релиз без совпадения контрольной суммы.

**Если обновление из 1.0.29 не проходит:** скачайте `PlanOperaciy-Windows.zip` с этой страницы вручную и распакуйте поверх папки программы (сохраните `calendars.json`, `credentials.json`, `token.pickle`).
